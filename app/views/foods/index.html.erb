<h1>Foods</h1>
<%= link_to 'Volver atrás', category_subcategories_path(@subcategory.category) %>
<br><br>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Código</th>
      <th>Medida casera</th>
      <th>Calorias</th>
      <th>Proteinas (g)</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.code %></td>
        <td><%= food.homemade_portion %></td>
        <td><%= food.calories %></td>
        <td><%= food.proteins_g %></td>
        <% if @recipe %>
        <td><%= link_to 'Añadir a la receta actual', recipe_food_compounds_path(@recipe, food), method: :post %></td>
        <% else %>
        <td><%= link_to 'Añadir en una receta nueva', food_recipes_path(food), method: :post %></td>
        <% end %>
        <td><%= link_to 'Edit', edit_food_path(food) %></td>
        <td><%= link_to 'Destroy', food, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
